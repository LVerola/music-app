{
  "name": "music-app",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "postinstall": "pnpm --dir frontend install && dotnet restore backend/MusicApp.slnx",
    "dev:api": "dotnet watch run --project backend/src/MusicApp.Api",
    "dev:web": "pnpm --dir frontend dev",
    "dev": "docker compose up postgres -d --wait && pnpm run \"/^dev:/\""
  }
}
